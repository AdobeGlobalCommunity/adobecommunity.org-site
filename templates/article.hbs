<div class="row my-3">
    <div class="col-4">
        <a href="{{url}}">
            <img class="img-fluid" src="{{image}}" alt="{{title}}" />
        </a>
    </div>
    <div class="col-8">
        <div class="lead" >
            <a href="{{url}}">
                {{title }}
            </a>
        </div>
        <em>
            By {{ author }} on {{ date }}
        </em><br/>
        <a href="{{url}}" class="btn btn-primary">Read More</a>
    </div>
</div>